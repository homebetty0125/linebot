{
  "name": "linebot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "PORT=3005 && nodemon start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "linebot": "^1.6.1",
    "nodemon": "^2.0.7"
  }
}